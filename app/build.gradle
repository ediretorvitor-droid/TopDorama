plugins {
    id 'com.android.application'
}

android {
    namespace 'com.topdorama.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.topdorama.app'
        minSdk 24
        targetSdk 35
        versionCode 2
        versionName '0.2.0-test'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
