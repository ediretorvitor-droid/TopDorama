plugins {
    id 'com.android.application'
}

android {
    namespace 'com.topdorama.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.topdorama.mobiletest'
        minSdk 24
        targetSdk 35
        versionCode 7
        versionName '0.7.0-test-data-safety'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
